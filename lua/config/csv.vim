" no keymaps, way too many clashes
let g:no_plugin_maps = 1

" different highlighting for the header
let g:csv_hiHeader = 'Pmenu'

" Highligth the column where the cursor is
let g:csv_highlight_column = 'y'

" choose delimiter to use, speed up
let g:csv_start = 1
let g:csv_end = 1000

" left align when arranging/tabularizing
let g:csv_arrange_align = 'l*'

" comments
let g:csv_comment = '#'
